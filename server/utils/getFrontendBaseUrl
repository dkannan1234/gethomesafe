function getFrontendBaseUrl() {
  // falls back to localhost:5173 (Vite dev server).
  return (
    process.env.FRONTEND_URL ||
    "http://localhost:5173"
  );
}

module.exports = {
  getFrontendBaseUrl,
};
